language: node_js
node_js:
  - "8"
sudo: required

services:
  - docker

after_deploy:
  - echo "done deploying"


#language: generic
#sudo: required
#services:
#        - docker

          #before_install:
          #        - docker build -t tcesario/simpleweb -f Dockerfile .

deploy:
        edge: true
        provider: elasticbeanstalk
        region: "us-east-2"
        app: "simpleweb"
        env: "Simpleweb-env"
        bucket_name: "elasticbeanstalk-us-east-2-870598663126"
        bucket_path: "docker"
        on:
                branch: master
        access_key_id: $AWS_ACCESS_KEY
        secret_access_key: $AWS_SECRET_KEY

        #after_install:
        #        - docker run -p 8080:8080 tcesario/simpleweb
